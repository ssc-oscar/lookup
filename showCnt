#!/bin/bash

h=$(hostname)
t=$1

fmt=$2

cfmt=${fmt:-3}

if [[ $h == da5.eecs.utk.edu ]] 
then 
  ~/lookup/showCnt.perl $t $cfmt
else
 if [[ $h == 'da3.eecs.utk.edu' || $h == 'da4.eecs.utk.edu' ]] 
 then ~/lookup/showCnt.perl $t $cfmt
 else ssh da4 ~/lookup/showCnt.perl $t $cfmt
 fi
fi
